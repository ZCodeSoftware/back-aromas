APP_NAME=
PORT=
APP_API_KEY=
APP_GLOBAL_PREFIX=
FRONT_BASE_URL=
JWT_SECRET=
# Token lifetime, e.g. 15m / 12h / 1d. Defaults to 1d when empty: leaving it
# blank used to issue tokens that never expire.
JWT_EXPIRATION_TIME=1d
APP_DOMAIN=
NODE_ENV=
MONGO_URI=
# Fixed UTC offset the analytics reports use to cut calendar days. Defaults to -03:00.
ANALYTICS_TZ_OFFSET=-03:00